export default interface ThemeInterface {
  animDelay?: number;
  primary: string;
  secondary: string;
  tertiary: string;
  contentColor: string;
  labelColor: string;
  textColor: string;
  isSelected?: boolean;
  active?: boolean;
  isOn?: boolean;
  width?: number;
}